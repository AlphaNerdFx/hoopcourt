# Development and CI tooling.
-r requirements.txt
ruff~=0.8
pytest~=8.4
pytest-cov~=6.0

# starlette.testclient, which tests/test_api.py uses, imports httpx at module
# load and raises if it is absent. It was never declared: the developer venv was
# created with --system-site-packages and resolved httpx from ~/.local, so the
# suite passed locally for months while a clean install could not collect
# tests/test_api.py at all. CI found it on the first run against the remote.
httpx~=0.28

# Mutation testing, run per pull request against the files a branch changed.
# Pinned to 2.5.1: mutmut 3.x asserts internally that module names do not start
# with `src.`, which every import in this repository does, so it cannot run here.
mutmut==2.5.1
